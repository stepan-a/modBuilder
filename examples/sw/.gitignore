# Artefacts regenerated by sw.m and the Dynare run
# (*.mod and *.pdf are already ignored repository-wide)
+sw/
sw/
*.log
*_ssblock_*.m

# Development scripts, kept in the working tree but out of the published
# example: sw_nosteady.m is a diagnostic comparing the declaration pairing
# with the one matchequations infers, and the validate_* scripts are ad-hoc
# checks of the ARMA helpers rather than example material.
sw_nosteady.m
matlab/validate_*.m
